const sum = (n1, n2) => {
  return n1 + n2;
}

const divide = (n1, n2) => {
  return n1 / n2;
}

const calc = (n1, n2) => {
  return ((n1 + n2) * 15) / 100;
}

export default calc;
export {
  sum,
  divide
}